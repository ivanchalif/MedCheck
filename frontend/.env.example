# No frontend environment variables required.
# API access is protected by server-side session cookies.
# Set ACCESS_PASSWORD in backend/.env.local to enable password protection.
# Auth endpoints: POST /api/auth/login, GET /api/auth/status, POST /api/auth/logout
